using System;
using System.Collections.Generic;
using System.Text;

namespace multisLib
{
    class ModPack
    {
        private string name, author, version, url, dir, mcVersion, serverUrl, logoName, imageName, info, animation, xml, isOp;
        private string[] mods;
        private string image, logo;

    }
}
